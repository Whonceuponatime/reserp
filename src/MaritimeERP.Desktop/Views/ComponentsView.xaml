<UserControl x:Class="MaritimeERP.Desktop.Views.ComponentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Styles -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#F44336"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#C62828"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InverseVisibilityStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEditing}" Value="False">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="EditingVisibilityStyle" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Compact Header -->
        <Border Grid.Row="0" 
                Background="White" 
                Padding="16,10"
                BorderThickness="0,0,0,1"
                BorderBrush="#E0E0E0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Components" 
                               FontSize="20" 
                               FontWeight="SemiBold"
                               Foreground="#1976D2"/>
                    <TextBlock Text="Manage ship components and equipment"
                               FontSize="12"
                               Foreground="#757575"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="+ Add Component" 
                            Command="{Binding AddComponentCommand}"
                            Background="#1976D2"
                            Foreground="White"
                            BorderThickness="0"
                            Height="32"
                            Padding="16,0"
                            FontSize="12"
                            Margin="0,0,8,0"/>
                    <Button Content="🗂️ Import" 
                            Background="#424242"
                            Foreground="White"
                            BorderThickness="0"
                            Height="32"
                            Padding="12,0"
                            FontSize="12"
                            Margin="0,0,8,0"/>
                    <Button Content="📊 Export" 
                            Background="#424242"
                            Foreground="White"
                            BorderThickness="0"
                            Height="32"
                            Padding="12,0"
                            FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Compact Filters -->
        <Border Grid.Row="1" 
                Background="White" 
                Padding="16,8"
                BorderThickness="0,0,0,1"
                BorderBrush="#E0E0E0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Height="26"
                         Padding="8,0"
                         FontSize="12"
                         Background="#F5F5F5"
                         BorderThickness="0"/>
                
                <ComboBox Grid.Column="1" 
                          ItemsSource="{Binding Ships}"
                          SelectedItem="{Binding SelectedShip}"
                          Height="26"
                          Padding="8,0"
                          FontSize="12"
                          Background="#F5F5F5"
                          BorderThickness="0"
                          Margin="8,0,0,0"/>
                
                <ComboBox Grid.Column="2" 
                          ItemsSource="{Binding Systems}"
                          SelectedItem="{Binding SelectedSystem}"
                          Height="26"
                          Padding="8,0"
                          FontSize="12"
                          Background="#F5F5F5"
                          BorderThickness="0"
                          Margin="8,0,0,0"/>

                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Active Only" 
                               FontSize="11"
                               Foreground="#757575"
                               VerticalAlignment="Center"/>
                    <CheckBox IsChecked="{Binding ShowActiveOnly}"
                              Margin="6,0,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content with DataGrid -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- Components DataGrid -->
            <DataGrid Grid.Column="0"
                      ItemsSource="{Binding FilteredComponents}"
                      SelectedItem="{Binding SelectedComponent}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      Background="White"
                      BorderThickness="0"
                      RowHeight="26"
                      FontSize="12"
                      Margin="0,0,8,0">
                
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F5F5F5"/>
                        <Setter Property="Foreground" Value="#424242"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="Height" Value="32"/>
                        <Setter Property="Padding" Value="8,0"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F8F9FA"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Component Name" 
                                        Binding="{Binding Name}"
                                        Width="120"
                                        MinWidth="100"/>
                    <DataGridTextColumn Header="Maker"
                                        Binding="{Binding Maker}"
                                        Width="100"
                                        MinWidth="80"/>
                    <DataGridTextColumn Header="Model"
                                        Binding="{Binding Model}"
                                        Width="100"
                                        MinWidth="80"/>
                    <DataGridTextColumn Header="Ship"
                                        Binding="{Binding Ship.Name}"
                                        Width="100"
                                        MinWidth="80"/>
                    <DataGridTextColumn Header="System"
                                        Binding="{Binding System.Name}"
                                        Width="100"
                                        MinWidth="80"/>
                    <DataGridTextColumn Header="Location"
                                        Binding="{Binding Location}"
                                        Width="100"
                                        MinWidth="80"/>
                    <DataGridTextColumn Header="USB"
                                        Binding="{Binding USBPorts}"
                                        Width="50"
                                        MinWidth="40"/>
                    <DataGridTextColumn Header="LAN"
                                        Binding="{Binding LANPorts}"
                                        Width="50"
                                        MinWidth="40"/>
                    <DataGridTextColumn Header="Serial"
                                        Binding="{Binding SerialNumber}"
                                        Width="120"
                                        MinWidth="100"/>
                    <DataGridCheckBoxColumn Header="Remote"
                                            Binding="{Binding IsRemoteAccessible}"
                                            Width="60"
                                            MinWidth="50"/>
                    <DataGridTextColumn Header="Connected CBs"
                                        Binding="{Binding ConnectedCircuitBreakers}"
                                        Width="110"
                                        MinWidth="90"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Component Details Panel -->
            <Border Grid.Column="1" 
                    Background="White"
                    BorderThickness="1,0,0,0"
                    BorderBrush="#E0E0E0"
                    Padding="16">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Component Details" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="#1976D2"
                                   Margin="0,0,0,12"/>
                        
                        <!-- Component Info -->
                        <Grid Margin="0,0,0,12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Name:" FontSize="11" FontWeight="SemiBold" Foreground="#424242"/>
                            <TextBlock Grid.Row="0" Text="{Binding SelectedComponent.Name}" FontSize="12" Margin="0,0,0,6" HorizontalAlignment="Right"/>

                            <TextBlock Grid.Row="1" Text="Maker:" FontSize="11" FontWeight="SemiBold" Foreground="#424242"/>
                            <TextBlock Grid.Row="1" Text="{Binding SelectedComponent.Maker}" FontSize="12" Margin="0,0,0,6" HorizontalAlignment="Right"/>

                            <TextBlock Grid.Row="2" Text="Model:" FontSize="11" FontWeight="SemiBold" Foreground="#424242"/>
                            <TextBlock Grid.Row="2" Text="{Binding SelectedComponent.Model}" FontSize="12" Margin="0,0,0,6" HorizontalAlignment="Right"/>

                            <TextBlock Grid.Row="3" Text="Ship:" FontSize="11" FontWeight="SemiBold" Foreground="#424242"/>
                            <TextBlock Grid.Row="3" Text="{Binding SelectedComponent.Ship.Name}" FontSize="12" Margin="0,0,0,6" HorizontalAlignment="Right"/>

                            <TextBlock Grid.Row="4" Text="System:" FontSize="11" FontWeight="SemiBold" Foreground="#424242"/>
                            <TextBlock Grid.Row="4" Text="{Binding SelectedComponent.System.Name}" FontSize="12" Margin="0,0,0,6" HorizontalAlignment="Right"/>

                            <TextBlock Grid.Row="5" Text="Location:" FontSize="11" FontWeight="SemiBold" Foreground="#424242"/>
                            <TextBlock Grid.Row="5" Text="{Binding SelectedComponent.Location}" FontSize="12" Margin="0,0,0,6" HorizontalAlignment="Right"/>

                            <TextBlock Grid.Row="6" Text="Status:" FontSize="11" FontWeight="SemiBold" Foreground="#424242"/>
                            <TextBlock Grid.Row="6" Text="{Binding SelectedComponent.Status}" FontSize="12" Margin="0,0,0,6" HorizontalAlignment="Right"/>
                        </Grid>

                        <!-- Action Buttons -->
                        <StackPanel Orientation="Vertical" Margin="0,12,0,0">
                            <Button Content="Edit Component" 
                                    Command="{Binding EditComponentCommand}"
                                    Background="#1976D2"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Height="32"
                                    FontSize="12"
                                    Margin="0,0,0,6"/>
                            <Button Content="Delete Component" 
                                    Command="{Binding DeleteComponentCommand}"
                                    Background="#D32F2F"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Height="32"
                                    FontSize="12"
                                    Margin="0,0,0,6"/>
                            <Button Content="View History" 
                                    Background="#424242"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Height="32"
                                    FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Compact Status Bar -->
        <Border Grid.Row="3" Background="#F5F5F5" CornerRadius="2" Margin="0,8,0,0" Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Text="{Binding StatusMessage}" 
                           FontSize="10" 
                           Foreground="#757575" 
                           VerticalAlignment="Center"/>
                
                <ProgressBar Grid.Column="1" 
                             Width="80" 
                             Height="12" 
                             IsIndeterminate="True" 
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl> 