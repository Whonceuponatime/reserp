<Window x:Class="MaritimeERP.Desktop.Views.DocumentUploadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Upload Document" 
        Height="600" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Height" Value="35"/>
        </Style>
        
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Height" Value="35"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="35"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Upload Document" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <Separator/>
        </StackPanel>
        
        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="0,10,0,0">
                
                <!-- File Information -->
                <GroupBox Header="File Information" Padding="10" Margin="0,0,0,15">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Label Grid.Row="0" Grid.Column="0" Content="File Name:" Width="80"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding FileName}" 
                                   VerticalAlignment="Center" FontWeight="Medium"/>
                        
                        <Label Grid.Row="1" Grid.Column="0" Content="File Size:" Width="80"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding FileSize}" 
                                   VerticalAlignment="Center" FontWeight="Medium"/>
                        
                        <Label Grid.Row="2" Grid.Column="0" Content="File Type:" Width="80"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding FileType}" 
                                   VerticalAlignment="Center" FontWeight="Medium"/>
                    </Grid>
                </GroupBox>
                
                <!-- Document Metadata -->
                <GroupBox Header="Document Details" Padding="10" Margin="0,0,0,15">
                    <StackPanel>
                        <Label Content="Document Name *"/>
                        <TextBox Text="{Binding DocumentName, UpdateSourceTrigger=PropertyChanged}" 
                                 ToolTip="Enter a descriptive name for this document"/>
                        
                        <Label Content="Description"/>
                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                                 Height="60" 
                                 TextWrapping="Wrap" 
                                 VerticalScrollBarVisibility="Auto"
                                 AcceptsReturn="True"
                                 ToolTip="Provide additional details about this document"/>
                        
                        <Label Content="Category *"/>
                        <ComboBox ItemsSource="{Binding DocumentCategories}" 
                                  SelectedItem="{Binding SelectedCategory}"
                                  DisplayMemberPath="DisplayName"
                                  ToolTip="Select the appropriate document category"/>
                        
                        <Label Content="Ship Assignment"/>
                        <ComboBox ItemsSource="{Binding Ships}" 
                                  SelectedItem="{Binding SelectedShip}"
                                  DisplayMemberPath="ShipName"
                                  ToolTip="Assign this document to a specific ship (optional)"/>
                    </StackPanel>
                </GroupBox>
                
                <!-- Validation Status -->
                <GroupBox Header="Validation Status" Padding="10">
                    <StackPanel>
                        <TextBlock Text="{Binding ValidationMessage}" 
                                   FontWeight="Medium"
                                   Foreground="{Binding IsValid, Converter={StaticResource BoolToColorConverter}}"/>
                        
                        <Button Content="Validate File" 
                                Command="{Binding ValidateFileCommand}" 
                                Margin="0,10,0,0"
                                Width="120"
                                HorizontalAlignment="Left"/>
                    </StackPanel>
                </GroupBox>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Upload" 
                    Command="{Binding UploadCommand}"
                    IsDefault="True"
                    Background="#4CAF50"
                    Foreground="White"
                    Width="100">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="IsEnabled" Value="{Binding CanUpload}"/>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}"
                    IsCancel="True"
                    Background="#F44336"
                    Foreground="White"
                    Width="100"/>
        </StackPanel>
        
        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsUploading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="10" Margin="0,0,0,10"/>
                <TextBlock Text="Uploading document..." Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window> 