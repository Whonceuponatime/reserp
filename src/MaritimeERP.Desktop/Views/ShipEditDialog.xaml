<Window x:Class="MaritimeERP.Desktop.Views.ShipEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ship Editor" 
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">
    
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="🚢 Ship Editor" 
                       FontSize="18" 
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
            
            <TextBlock Text="Ship editing functionality will be implemented here." 
                       FontSize="14" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Save" 
                        Width="80"
                        Height="30"
                        Margin="10"
                        Click="Save_Click"/>
                <Button Content="Cancel" 
                        Width="80"
                        Height="30"
                        Margin="10"
                        Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window> 